- hosts: bootstrap
  tasks:
  - name: grab the json
    uri: url=https://ci.jenkins-ci.org/job/core_selenium-test/api/json HEADER_Content-Type="application/json" return_content=yes
    register: returned_json

  - debug: var=returned_json

